# ============================================================================
# EXEMPLO DE VARIÁVEIS DE AMBIENTE PARA PRODUÇÃO (RENDER)
# ============================================================================
# Copie este arquivo e configure as variáveis no Render:
# Dashboard > Service > Environment > Add Environment Variable
# ============================================================================

# DATABASE - PostgreSQL (Render fornece automaticamente DATABASE_URL)
# Opção 1: Usar DATABASE_URL do Render diretamente
SPRING_DATASOURCE_URL=jdbc:postgresql://dpg-xxxxx.oregon-postgres.render.com:5432/seu_database

# Opção 2: Ou configure separadamente
SPRING_DATASOURCE_USERNAME=seu_usuario
SPRING_DATASOURCE_PASSWORD=sua_senha_segura

# JWT SECRET - ALTERE ESTE VALOR PARA UM ALEATÓRIO E SEGURO!
# Gere com: .\generate-jwt-secret.ps1
# Ou: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=SuaChaveSecretaSuperSeguraDeNoMinimo256BitsParaHS256Algorithm

# JWT TOKEN EXPIRATION (opcional - valores padrão já configurados)
# Access Token: 15 minutos = 900000ms
# Refresh Token: 7 dias = 604800000ms
JWT_ACCESS_TOKEN_EXPIRATION=900000
JWT_REFRESH_TOKEN_EXPIRATION=604800000

# SPRING PROFILE (já configurado no Dockerfile)
SPRING_PROFILES_ACTIVE=prod

# HIBERNATE DDL AUTO (controla criação/atualização de tabelas)
# Valores: validate | update | create | create-drop | none
# - validate: apenas valida schema (produção segura)
# - update: cria/atualiza tabelas automaticamente (primeiro deploy)
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# CORS (Cross-Origin Resource Sharing)
# Origens permitidas separadas por vírgula
# Exemplos:
# - * (qualquer origem - apenas para desenvolvimento!)
# - https://meuapp.vercel.app
# - https://meuapp.vercel.app,https://www.meuapp.com
CORS_ALLOWED_ORIGINS=*

# USUÁRIO ADMIN PADRÃO (criado automaticamente no primeiro boot)
# Altere as credenciais padrão para mais segurança
DEFAULT_USER_EMAIL=admin@lalurecf.com.br
DEFAULT_USER_FIRST_NAME=Admin
DEFAULT_USER_LAST_NAME=Sistema
DEFAULT_USER_PASSWORD=Admin@123

# ============================================================================
# CONFIGURAÇÃO NO RENDER
# ============================================================================
# 1. Crie um PostgreSQL database no Render
# 2. Copie a "Internal Database URL" (dpg-xxxxx.oregon-postgres.render.com)
# 3. Configure as variáveis de ambiente no seu Web Service:
#    - SPRING_DATASOURCE_URL: cole a URL interna (jdbc:postgresql://...)
#    - SPRING_DATASOURCE_USERNAME: usuário do banco
#    - SPRING_DATASOURCE_PASSWORD: senha do banco
#    - JWT_SECRET: chave secreta gerada
#
# IMPORTANTE: Use a URL INTERNA do banco (dpg-xxxxx.oregon-postgres.render.com)
# NÃO use a URL externa (xxxxxx.proxy.rlwy.net ou similar)
# ============================================================================
